@page
@model MelisaIuliaProiect.Pages.VehicleModels.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>VehicleModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="VehicleModel.ID" />
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="VehicleModel.VehicleModelName" class="control-label"></label>
                <input asp-for="VehicleModel.VehicleModelName" class="form-control" />
                <span asp-validation-for="VehicleModel.VehicleModelName" class="text-danger"></span>
            </div>
            <br/>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
